import { TestCase, TestResult } from '@playwright/test/reporter';
import { TestResultType } from 'qase-javascript-commons';
import { ReporterOptionsType } from './options';
import { StepConverter } from './step-converter';
import { TestCaseMetadata } from './metadata-extractor';
export interface BuildArgs {
    test: TestCase;
    result: TestResult;
    metadata: TestCaseMetadata;
    annotations: {
        ids: number[];
        projectMapping: Record<string, number[]> | null;
        suites: string[];
    };
    options: ReporterOptionsType;
    isCaptureLogs: boolean;
    qaseIdsRegistry: ReadonlyMap<string, number[]>;
}
export declare class ResultBuilder {
    private readonly stepConverter;
    constructor(stepConverter: StepConverter);
    build(args: BuildArgs): TestResultType | null;
}
