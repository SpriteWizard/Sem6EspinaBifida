"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AnnotationExtractor = void 0;
const internal_1 = require("qase-javascript-commons/internal");
class AnnotationExtractor {
    extractQaseIds(annotations) {
        const ids = [];
        for (const item of annotations) {
            if (item.type.toLowerCase() === 'qaseid' && item.description) {
                ids.push(...(0, internal_1.parseQaseIdsFromString)(item.description));
            }
        }
        return ids;
    }
    extractProjectMapping(annotations) {
        for (const item of annotations) {
            if (item.type.toLowerCase() === 'qaseprojects' && item.description) {
                try {
                    const parsed = JSON.parse(item.description);
                    if (parsed && typeof parsed === 'object' && Object.keys(parsed).length > 0) {
                        return parsed;
                    }
                }
                catch {
                    // ignore invalid JSON
                }
            }
        }
        return null;
    }
    extractSuite(annotations) {
        const suites = [];
        for (const item of annotations) {
            if (item.type.toLowerCase() === 'qasesuite' && item.description) {
                suites.push(item.description);
            }
        }
        return suites;
    }
}
exports.AnnotationExtractor = AnnotationExtractor;
